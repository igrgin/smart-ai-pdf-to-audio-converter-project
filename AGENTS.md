## Agent skills

### Issue tracker

Issues are tracked with GitHub Issues; external pull requests are not a triage surface. See `docs/agents/issue-tracker.md`.

### Triage labels

The repository uses the five default triage labels. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository. See `docs/agents/domain.md`.

### Engineering contract

Before implementation, read `docs/agents/engineering-contract.md` and every contract document it marks as applicable to the work. Resolve conflicts with the user before writing code.
