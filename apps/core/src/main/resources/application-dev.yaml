spring:
  datasource:
    hikari:
      maximum-pool-size: 2

server:
  servlet:
    session:
      cookie:
        secure: false

platform:
  identity:
    secure-session-cookie: false

management:
  tracing:
    sampling:
      probability: 1.0
